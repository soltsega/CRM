# Mini CRM Project

A simple client lead management system (CRM) with React, Node.js/Express, and MongoDB.

## Structure
- `backend/`: Express API and business logic
- `frontend/`: React dashboard

See `task_description.md` for requirements.
